// Package server hosts the Echo v5 web server: ConnectRPC handlers
// (binary protocol), embedded SPA serving via go:embed, JWT cookie
// session interceptors, and security middleware. Introduced in M7.
package server
