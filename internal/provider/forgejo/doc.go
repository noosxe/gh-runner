// Package forgejo implements the GitProvider interface for Forgejo:
// PAT authentication, registration-token API, and queued-job polling
// (Forgejo lacks workflow_job webhooks). Introduced in RUN-27.
package forgejo
