// Package provider defines the swappable GitProvider interface decoupling
// the supervisor engine from specific VCS APIs (registration tokens,
// credential validation, scaling mode, queued-job polling). See
// docs/02-architecture-design.md §3.2.
package provider
