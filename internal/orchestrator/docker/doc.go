// Package docker implements the ContainerProvider interface against the
// Docker Engine API via the official Go SDK (DOCKER_HOST socket or remote
// TCP+TLS). Introduced in M5 (RUN-30..RUN-36).
package docker
